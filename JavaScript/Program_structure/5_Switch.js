// much like control flow one (if elseif and else) they can be used
// syntax is---

/*
switch (prompt('What is the weather like?')){
    case 'rainy':
        alert('Remember to bring an umbrella')
        break
    
    case 'sunny':
        alert('Dress lightly')
    
    case 'cloudy':
        alert('Go outside')
    
    default:
        alert('Unknown weather type!)
}

We can use break if we wish to end these cases after one case in met otherwise
all the cases after that will also be executed!
*/