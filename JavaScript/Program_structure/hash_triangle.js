// with for loop

for (let counter = 0, hash = ''; counter < 7; counter += 1) {
    hash += '#';
    console.log(hash);
}

// with while loop

i = 0;
hash = '';
while (i < 7) {
    hash += '#';
    console.log(hash);
    i += 1;
}