/*
Type of values in Js
1. Numbers 
2. String
3. Boolean
4. Object
5. Function
6. Undefined
*/


// Numbers

// When for example we write and number say 13
// A bit pattern comes into existence in computer memory(and it gets recycled if not saved or deleted or used once)
// Js uses 64 bits to store one single number
// Which means that a single number can be as long as 2**64(converting binary into decimal number)
// But Due to non-whole numbers , decimal positioning and negative numbers the actual range is 2**9 
// Use average value with fraction due to precision problems


// String
// These are basically any character inside a single or double quote
// In js, string follows 'unicode standard'.
// A standard which gives every possible character, a number
// just like in number case,characters are saved in 8-bits

// Boolean 

// true , false
// 4>5 gives true
// 1<-3 gives false
// 3 == 4 gives false
// null === undefined gives false



